import { POST } from '@/axios';
import CommonService from '@/store/CommonService';
import API from '@/api/index';

class Service extends CommonService {
    // loginPhone(params) {
    //     return POST(API.loginPhone, params);
    // }

}

export default new Service();
